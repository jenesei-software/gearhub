<Window x:Class="GearHub.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="clr-namespace:Hardcodet.Wpf.TaskbarNotification;assembly=Hardcodet.NotifyIcon.Wpf"
        xmlns:vm="clr-namespace:GearHub.App.ViewModels"
        Title="GearHub"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        MinWidth="300"
        MaxWidth="1000"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>
        <DataTemplate x:Key="DeviceCardTemplate" DataType="{x:Type vm:DeviceViewModel}">
            <Border x:Name="Card"
                    CornerRadius="10"
                    Background="{StaticResource CardBrush}"
                    Padding="10,7"
                    Margin="3">
                <Border.ContextMenu>
                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <MenuItem Header="Игнорировать" Command="{Binding IgnoreCommand}" />
                    </ContextMenu>
                </Border.ContextMenu>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Ellipse x:Name="StatusDot"
                             Grid.Column="0"
                             Width="8"
                             Height="8"
                             Fill="{StaticResource AccentGreenBrush}"
                             VerticalAlignment="Center"
                             Margin="0,0,8,0" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,10,0">
                        <TextBlock Text="{Binding Name}"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontSize="12.5"
                                   FontWeight="SemiBold"
                                   MaxWidth="180"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding Subtitle}"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="10.5"
                                   MaxWidth="180"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding BatteryGlyph}"
                                   FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   FontSize="13"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,4,0" />
                        <TextBlock Text="{Binding BatteryText}"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontSize="12"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Grid>
            </Border>

            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Attention">
                    <Setter TargetName="StatusDot" Property="Fill" Value="{StaticResource AccentYellowBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Lost">
                    <Setter TargetName="StatusDot" Property="Fill" Value="{StaticResource AccentRedBrush}" />
                    <Setter TargetName="Card" Property="Opacity" Value="0.55" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Border x:Name="Root"
                CornerRadius="14"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource PanelBorderBrush}"
                BorderThickness="1"
                Padding="10,8"
                ToolTip="{Binding StatusLine}"
                MouseLeftButtonDown="OnRootMouseDown">
            <Border.Effect>
                <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.45" Color="#000000" />
            </Border.Effect>
            <DockPanel>

                <StackPanel DockPanel.Dock="Right"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="8,0,0,0">
                    <Button Style="{StaticResource IconButtonStyle}"
                            Content="&#xE72C;"
                            ToolTip="Обновить сейчас"
                            Click="OnRefreshClick" />
                    <Button Style="{StaticResource IconButtonStyle}"
                            Content="&#xE8BB;"
                            ToolTip="Свернуть в трей"
                            Margin="2,0,0,0"
                            Click="OnHideClick" />
                </StackPanel>

                <Grid>
                    <ItemsControl ItemsSource="{Binding Devices}"
                                  ItemTemplate="{StaticResource DeviceCardTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>

                    <TextBlock Text="Устройства не найдены"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="12"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Margin="20,10">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasDevices}" Value="False">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

            </DockPanel>
        </Border>

        <tb:TaskbarIcon x:Name="Tray"
                        IconSource="{StaticResource TrayIconImage}"
                        ToolTipText="GearHub — заряд устройств"
                        TrayMouseDoubleClick="OnTrayDoubleClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Показать / скрыть" Click="OnToggleWindowClick" />
                    <MenuItem Header="Обновить" Click="OnRefreshClick" />
                    <Separator />
                    <MenuItem Header="Выход" Click="OnExitClick" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</Window>
